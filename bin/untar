#!/bin/bash
# Extract an archive (modern tar auto-detects compression). Bare .gz uses gzip -d.

case "$1" in
    *.tar|*.tar.gz|*.tgz|*.tar.bz2|*.tbz2) tar xvf "$1" ;;
    *.gz) gzip -d "$1" ;;
    *) cat "$(command -v untar)" ;;
esac
