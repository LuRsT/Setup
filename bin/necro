#!/bin/fish
# fzf-pick a file, then fzf its git log with a preview of each revision.

set FILE (fzf)
git log --oneline --follow -- $FILE | fzf --color --preview "git show (echo {} | cut -d ' ' -f 1):$FILE"
