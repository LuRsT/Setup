#!/bin/bash

# Updates every single git repository inside `pwd`

for i in */.git ; do
    DIR_NAME=$(echo "$i" | sed 's/\/.*//');
    cd $DIR_NAME;
    echo "Updating ${DIR_NAME}...";
    git pull;
    cd ..;
done
